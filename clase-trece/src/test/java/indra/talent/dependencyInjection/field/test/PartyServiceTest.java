package indra.talent.dependencyInjection.field.test;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

import indra.talent.dependenciesInjection.field.MockRepository;
import indra.talent.dependenciesInjection.field.PartyService;

public class PartyServiceTest {

	
	/*@Test
	public void test() {
		PartyService service = new PartyService();
		service.setRepository(new MockRepository());
		//service.setRepository(new MockRepository());
		service.haveFun();

		assertEquals(true, true);
		;

	}*/
}
